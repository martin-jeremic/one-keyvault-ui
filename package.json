{
  "name": "one-keyvault-ui",
  "displayName": "One Key Vault UI",
  "publisher": "onedevteam",
<<<<<<< HEAD
  "description": "Browse and edit Azure Key Vault secrets directly in VS Code with an intuitive explorer UI",
  "version": "0.0.1",
=======
  "description": "Azure Key Vault explorer with inline secret editing capabilities",
  "version": "1.0.0",
>>>>>>> main
  "engines": {
    "vscode": "^1.98.2"
  },
  "icon": "img/one-keyvault-ui-logo-transparent.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/martin-jeremic/one-keyvault-ui.git"
  },

  "categories": [
    "Azure",
    "Other"
  ],
  "keywords": [
    "azure",
    "key vault",
    "azure key vault",
    "secrets",
    "secret management",
    "devops",
    "cloud",
    "security",
    "vscode",
    "keyvault",
    "credential management",
    "secret editor"
  ],
  "activationEvents": [
    "onCommand:oneKeyVault.openKeyVaultView"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "oneKeyVault.openKeyVaultView",
        "title": "Open Key Vault UI",
        "icon": "$(cloud)"
      },
      {
        "command": "oneKeyVault.addKeyVault",
        "title": "Add Key Vault",
        "icon": "$(add)"
      },
      {
        "command": "oneKeyVault.removeKeyVault",
        "title": "Remove Key Vault",
        "icon": "$(trash)"
      },
      {
        "command": "oneKeyVault.refresh",
        "title": "Refresh",
        "icon": "$(refresh)"
      },
      {
        "command": "oneKeyVault.clearStoredCredentials",
        "title": "Clear Stored Credentials",
        "icon": "$(key-remove)"
      }
    ],
    "menus": {
      "view/title": [
        {
          "command": "oneKeyVault.refresh",
          "when": "view == oneKeyVaultTree",
          "group": "navigation"
        }
      ],
      "view/item/context": [
        {
          "command": "oneKeyVault.removeKeyVault",
          "when": "viewItem == keyVault",
          "group": "inline"
        },
        {
          "command": "oneKeyVault.clearStoredCredentials",
          "when": "viewItem == keyVault",
          "group": "inline"
        }
      ]
    },
    "viewsContainers": {
      "activitybar": [
        {
          "id": "oneKeyVaultContainer",
          "title": "Azure Key Vault",
          "icon": "$(cloud)"
        }
      ]
    },
    "views": {
      "oneKeyVaultContainer": [
        {
          "id": "oneKeyVaultTree",
          "name": "Key Vaults",
          "type": "tree"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run esbuild-base -- --minify",
    "esbuild-base": "esbuild ./src/extension.ts --bundle --outfile=out/extension.js --external:vscode --format=cjs --platform=node",
    "esbuild": "npm run esbuild-base -- --sourcemap",
    "esbuild-watch": "npm run esbuild-base -- --sourcemap --watch",
    "compile": "tsc -p ./ && npm run webview:build",
    "watch": "tsc -watch -p ./",
    "webview:build": "tsc -p ./media/tsconfig.webview.json",
    "webview:watch": "tsc -watch -p ./media/tsconfig.webview.json",
    "lint": "eslint src",
    "pretest": "npm run compile && npm run lint",
    "test": "node ./out/test/runTest.js",
    "release": "standard-version",
    "release:minor": "standard-version --release-as minor",
    "release:major": "standard-version --release-as major"
  },
  "devDependencies": {
    "@semantic-release/changelog": "^6.0.3",
    "@semantic-release/commit-analyzer": "^13.0.1",
    "@semantic-release/exec": "^7.1.0",
    "@semantic-release/git": "^10.0.1",
    "@semantic-release/github": "^12.0.6",
    "@semantic-release/npm": "^13.1.4",
    "@semantic-release/release-notes-generator": "^14.1.0",
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.85.0",
    "@typescript-eslint/eslint-plugin": "^6.0.0",
    "@typescript-eslint/parser": "^6.0.0",
    "@vscode/vsce": "^3.2.1",
    "esbuild": "^0.19.0",
    "eslint": "^8.0.0",
    "semantic-release": "^25.0.3",
    "standard-version": "^9.5.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@azure/identity": "^4.0.0",
    "@azure/keyvault-secrets": "^4.7.0"
  }
}
